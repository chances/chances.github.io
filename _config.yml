# Title, decription, tagline and URL for your site
# Can be used in your theme by calling 'site.title' and so on
title: Chance Snow
description: "Chance Snow's personal website, complete with a blog, portfolio, and more!"
tagline: Student, redhead, web developer
url: localhost:4000

# Used so Jekyll outputs the site correctly so Gulp can do what it wants
source: src
destination: serve

# Same as the title etc for your site but can instead be
# called by using 'site.author.name' and so on
author:
  name: Chance Snow
  email: chances@cat.pdx.edu
  bio: I'm a third year, junior undergraduate student of Computer Science at <a href="http://pdx.edu">Portland State University</a>. My interests lie mainly in full stack web development and software engineering.
  facebook: chancexsnow
  twitter: ChancesOfSnow
  google_plus: 113414184061295490862
  lastfm: EnigmaticEffigy
  linked_in: chancesnow
  tumblr: chancesnow
  github: chances
  npm: chancesnow
  dribbble: chance

# Default date format for site
date_format: '%B %-d, %Y'

# general setting for Jekyll
googleAnalytics: UA-XXXXX-X

# exclude uncompiled SASS from build
exclude: ['assets/scss']

# _config.build.yml overwrites these options when you run `gulp build`
# Enables future posts (posts with dates in the future) and drafts
future: true
show_drafts: true
# Disables the more accurate related posts for faster generating of the site
lsi: false
# Only make the last 10 posts so generating isn't slow
limit_posts: 10

# Permalink structure and pagination options
relative_permalinks: false
#relative_permalinks: true
permalink: /blog/:title
paginate: 10
paginate_path: 'blog/page:num'
excerpt_separator: '<!--more-->'

# Markdown library
markdown: kramdown
# Markdown library options
redcarpet:
  extensions: ['no_intra_emphasis', 'tables', 'fenced_code_blocks', 'autolink', 'smart',
              'strikethrough', 'superscript', 'underline', 'highlight', 'footnotes']
highlighter: rouge
